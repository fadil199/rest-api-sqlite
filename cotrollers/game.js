const Game = require("../models/game");

module.exports = {
    inputGame: async (req, res, next) => {
        try {
            
        } catch (err) {
            next(err)
        }
    },
    getAllGame: async (req, res, next) => {
        try {
            
        } catch (err) {
            next(err)
        }
    }
}